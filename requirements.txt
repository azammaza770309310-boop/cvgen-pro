# CVGen Pro — Python dependencies
#
# Production runtime: Reflex only (no FastAPI server).
# FastAPI/uvicorn/jinja2 are kept for test compatibility (properly skipped
# via conftest.py collect_ignore when Starlette version is incompatible).
# They are NOT imported by the Reflex production code.

# --- Production dependencies (used by Reflex + handlers) ---
reflex>=0.6.0
pydantic>=2.5
pydantic-settings>=2.1
weasyprint>=60
python-docx>=1.1
httpx>=0.26
pypdf>=4.0
sentry-sdk[fastapi]>=1.40

# --- Transitive dependencies (required by reflex, pinned for compatibility) ---
starlette>=0.36
python-multipart>=0.0.7

# --- Test-only dependencies (NOT used by Reflex production) ---
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1