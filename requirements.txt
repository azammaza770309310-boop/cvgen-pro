# CVGen Pro — Production Python dependencies
# Reflex is the ONLY production runtime. No FastAPI server.
#
# CRITICAL: Do NOT include fastapi or sentry-sdk[fastapi] here.
# Reflex 0.9.7 requires Starlette 1.3.1 which is incompatible with FastAPI.
# Including fastapi causes pip install conflicts during Docker build.

# --- Reflex + framework ---
reflex>=0.6.0

# --- Business logic (framework-independent) ---
pydantic>=2.5
pydantic-settings>=2.1
weasyprint>=60
python-docx>=1.1
httpx>=0.26
pypdf>=4.0

# --- Error tracking (plain, no [fastapi] extra) ---
sentry-sdk>=1.40

# --- Transitive (required by reflex, pinned for clarity) ---
starlette>=0.36
python-multipart>=0.0.7
